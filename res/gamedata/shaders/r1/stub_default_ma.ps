#include "common.h"
#include "common_iostructs.h"

//////////////////////////////////////////////////////////////////////////////////////////
// Pixel

float4 main ( p_TL I ) : COLOR
{
	float4 res = tex2D( s_base, I.Tex0 );
//	res.rgb		= lerp( I.Color.rgb, res.rgb, I.Color.a);
	res.rgb		= lerp( res.rgb, I.Color.rgb, I.Color.a);
	res.a		*= I.Color.a;

//	clip(res-m_AlphaRef);
//	clip(res-0.5);

	return res;
//	return float4(1,1,1,1);
}
